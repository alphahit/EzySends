// theme/colors.ts
// import {Appearance} from 'react-native';
import {
  lightColors,
  //  darkColors
} from './palettes';

// const scheme = Appearance.getColorScheme();
// This returns 'light' | 'dark' | null.
// null can be treated as 'light' by default.

//lightColors export const COLORS = scheme === 'dark' ? darkColors : lightColors;
export const COLORS = lightColors;
